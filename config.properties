# Map Editor config — FILE NGOÀI (sửa ở đây, KHÔNG cần build lại jar)
# File này override bản mặc định đóng trong jar. Đã .gitignore (không commit creds).

# Đường dẫn repo client (để đọc asset: prefab map, texture, spine atlas)
client.repo=E:/UocRongOnline/UocRongOnline-Client
# Thư mục asset bundle resource trong client (chứa Map prefab)
client.resource=Assets/AssetBundles/Resource
# Thư mục scan GUID index (rộng — material/texture nằm rải ngoài Resource)
client.assets=Assets

# Repo server (đọc enum ItemAttribute → tên option/buff cho Shop & Equip editor)
# Dùng junction ASCII E:/UocRongOnline (trỏ tới E:/Dự án Ước Rồng/Dự án Ước Rồng) — path tiếng Việt dễ hỏng
server.repo=E:/UocRongOnline/UocRongOnline-Server

# Gateway Spring (giftcode/login) — tool gọi {url}reload sau khi thêm/xóa giftcode
gateway.url=http://server.uocrong.vn:8001/home/

# DB
db.url=jdbc:mysql://45.119.215.17:3306/nro_restore?useSSL=false&serverTimezone=UTC
db.user=quanlygame
db.pass=123321123

# Render
render.pixelsPerUnit=100
